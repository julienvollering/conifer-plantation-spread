---
title: "eKlima stations"
output: html_document
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(here)
```

```{r}
active <- read_delim(here("data", "raw", "eKlima", "windstationsEklima.txt"), delim = ";", 
                     na=c("-9999"))
files <- list.files(here("data", "raw", "eKlima"), 
                         pattern = "metastations", full.names = TRUE)
read_delim(coordfiles, delim = ";")
metastations <- lapply(files, read_delim, delim = ";") %>% 
  bind_rows() %>% 
  select(Stnr:Name, Utm_e:Utm_zone) %>% 
  distinct(Stnr, .keep_all = TRUE)
  
metastations
```

